import { test } from '../Utilities/Fixtures.js';
import { expect } from '@playwright/test';
import PomManager from '../Pages/PomManager.js';
import ApiUtil from '../Utilities/ApiUtil.js';
import MockUtil from '../Utilities/MockUtil.js';
import { readCsvFile } from '../Utilities/ReadCsvFile.js';
const dashboardData = require('../Data/DashboardData.json');


test('Create and assign customer to dashboard', async ({page}) => {
    await test.step('Create device', async () => {

    })

    await test.step('Connect device', async () => {

    })

    await test.step('Create dashboard', async () => {

    })

    await test.step('Configure alarm rules', async () => {
        
    })

    await test.step('Create alarm', async () => {
        
    })

    await test.step('Create customer and assign to dashboard', async () => {
        
    })

    await test.step('Unassign customer from dashboard', async () => {
        
    })

    await test.step('Delete dashboard', async () => {
        
    })

    await test.step('Delete alarm', async () => {
        
    })

    await test.step('Reset alarm configuration', async () => {
        
    })

    await test.step('Delete device', async () => {

    })
    
    // 1 Create device
    // 2 Connect device
    // 3 Create dashboard
    // 4 Configure alarm rules
    // 5 Create alarm
    // 6 Create customer and assign to dashboard
    // 7 Unassign Customer to dashboard
    // 8 Delete dashboard
    // 9 Delete alarm
    // 10 Reset alarm configuration
    // 11 Delete device
    
})